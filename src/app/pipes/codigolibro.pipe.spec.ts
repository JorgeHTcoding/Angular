import { CodigolibroPipe } from './codigolibro.pipe';

describe('CodigolibroPipe', () => {
  it('create an instance', () => {
    const pipe = new CodigolibroPipe();
    expect(pipe).toBeTruthy();
  });
});
